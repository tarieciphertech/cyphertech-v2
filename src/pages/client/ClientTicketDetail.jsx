import { useCallback,useEffect,useState } from "react";
import { Link,useParams } from "react-router-dom";
import { FaHeadset,FaPaperPlane } from "react-icons/fa";
import { supabase } from "../../lib/supabase";
import { useAuth } from "../../auth/useAuth";
import { TICKET_PRIORITY_LABELS,TICKET_PRIORITY_STYLES,TICKET_STATUS_LABELS,TICKET_STATUS_STYLES,formatDateTime } from "./ticketBadges";
export default function ClientTicketDetail(){const{ticketId}=useParams();const{user}=useAuth();const[ticket,setTicket]=useState(null);const[messages,setMessages]=useState([]);const[loading,setLoading]=useState(true);const[notFound,setNotFound]=useState(false);const[loadError,setLoadError]=useState("");const[reply,setReply]=useState("");const[sending,setSending]=useState(false);const[replyError,setReplyError]=useState("");const loadMessages=useCallback(async(active=true)=>{if(!supabase||!ticketId)return;const{data,error}=await supabase.from("ticket_messages").select("id,author_id,body,created_at").eq("ticket_id",ticketId).order("created_at",{ascending:true});if(!active)return;if(error){console.error("Failed to load messages:",error.message);return}setMessages(data||[])},[ticketId]);useEffect(()=>{let active=true;async function loadTicket(){if(!supabase||!ticketId||!user){setLoading(false);return}const{data,error}=await supabase.from("tickets").select("id,subject,status,priority,created_at,updated_at").eq("id",ticketId).maybeSingle();if(!active)return;if(error){setLoadError("We could not load this ticket right now.");setLoading(false);return}if(!data){setNotFound(true);setLoading(false);return}setTicket(data);await loadMessages(active);setLoading(false)}loadTicket();return()=>{active=false}},[ticketId,user,loadMessages]);async function handleReply(event){event.preventDefault();setReplyError("");const trimmedBody=reply.trim();if(!supabase||!user||!ticketId){setReplyError("Replies are unavailable right now.");return}if(!trimmedBody){setReplyError("Please enter a message before sending.");return}setSending(true);const{error}=await supabase.from("ticket_messages").insert({ticket_id:ticketId,author_id:user.id,body:trimmedBody});setSending(false);if(error){console.error("Failed to send reply:",error.message);setReplyError("We could not send your message right now. Please try again.");return}setReply("");await loadMessages(true)}if(loading)return <div className="grid place-items-center py-24 text-white"><div className="flex flex-col items-center gap-4"><span className="h-8 w-8 animate-spin rounded-full border-2 border-cyan-300/30 border-t-cyan-300"/><span className="text-sm text-gray-400">Loading ticket...</span></div></div>;if(notFound)return <div className="card rounded-3xl p-8 text-center"><FaHeadset className="mx-auto text-2xl text-cyan-200"/><h2 className="mt-4 text-xl font-black text-white">Ticket not found or you don't have access to it.</h2><Link to="/client/tickets" className="btn btn-primary mt-6">Back to tickets</Link></div>;if(loadError||!ticket)return <div className="card rounded-3xl p-8 text-center"><p role="alert" className="text-sm font-semibold text-rose-200">{loadError||"We could not load this ticket right now."}</p><Link to="/client/tickets" className="btn btn-secondary mt-6">Back to tickets</Link></div>;return <div className="grid gap-6"><Link to="/client/tickets" className="text-sm font-bold text-cyan-300">← Back to tickets</Link><div className="card rounded-3xl p-6 md:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h1 className="text-2xl font-black text-white">{ticket.subject}</h1><p className="mt-2 text-xs text-gray-500">Created {formatDateTime(ticket.created_at)} · Updated {formatDateTime(ticket.updated_at)}</p></div><div className="flex gap-2"><span className={`rounded-full border px-3 py-1 text-xs font-black uppercase tracking-wide ${TICKET_STATUS_STYLES[ticket.status]||TICKET_STATUS_STYLES.open}`}>{TICKET_STATUS_LABELS[ticket.status]||ticket.status}</span><span className={`rounded-full border px-3 py-1 text-xs font-black uppercase tracking-wide ${TICKET_PRIORITY_STYLES[ticket.priority]||TICKET_PRIORITY_STYLES.normal}`}>{TICKET_PRIORITY_LABELS[ticket.priority]||ticket.priority}</span></div></div></div><div className="card rounded-3xl p-6 md:p-8"><h2 className="text-lg font-black text-white">Conversation</h2>{messages.length===0?<p className="mt-4 text-sm text-gray-400">No messages yet. Add details about your request below and our team will respond.</p>:<div className="mt-5 grid gap-4">{messages.map(m=><div key={m.id} className={`rounded-2xl border p-4 ${m.author_id===user?.id?"border-cyan-300/20 bg-cyan-300/5":"border-white/10 bg-white/[0.045]"}`}><div className="flex items-center justify-between gap-3"><span className="text-xs font-black uppercase tracking-wide text-gray-400">{m.author_id===user?.id?"You":"Support team"}</span><span className="text-xs text-gray-500">{formatDateTime(m.created_at)}</span></div><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-gray-200">{m.body}</p></div>)}</div>}<form onSubmit={handleReply} className="mt-6 grid gap-3 border-t border-white/10 pt-6"><label className="field-label">Add a reply<textarea rows={4} value={reply} onChange={e=>setReply(e.target.value)} className="field-input resize-none" placeholder="Add more detail or answer a question from our team..." required/></label>{replyError&&<p role="alert" className="rounded-xl border border-rose-300/30 bg-rose-300/10 px-4 py-3 text-sm font-semibold text-rose-200">{replyError}</p>}<button type="submit" disabled={sending} className="btn btn-primary"><FaPaperPlane className="mr-2"/>{sending?"Sending...":"Send Reply"}</button></form></div></div>}
