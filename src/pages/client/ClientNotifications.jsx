import { useEffect,useState } from "react";
import { FaBell,FaCheck,FaEnvelopeOpenText } from "react-icons/fa";
import { supabase } from "../../lib/supabase";
import { useAuth } from "../../auth/useAuth";
import { extractNotificationText,formatNotificationDate,formatNotificationType } from "./notificationHelpers";
export default function ClientNotifications(){const{user}=useAuth();const[notifications,setNotifications]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[markingId,setMarkingId]=useState(null);useEffect(()=>{let active=true;async function load(){if(!supabase||!user){setLoading(false);return}const{data,error:e}=await supabase.from("notifications").select("id,type,payload,read_at,created_at").order("created_at",{ascending:false});if(!active)return;if(e){setError("We could not load your notifications right now.");setNotifications([])}else setNotifications(data||[]);setLoading(false)}load();if(!supabase||!user)return()=>{};const channel=supabase.channel(`client-notifications-${user.id}`).on("postgres_changes",{event:"*",schema:"public",table:"notifications",filter:`user_id=eq.${user.id}`},load).subscribe();return()=>{active=false;supabase.removeChannel(channel)}},[user]);async function handleMarkRead(id){if(!supabase)return;setMarkingId(id);const readAt=new Date().toISOString();const{error:e}=await supabase.from("notifications").update({read_at:readAt}).eq("id",id).is("read_at",null);setMarkingId(null);if(e)return;setNotifications(current=>current.map(item=>item.id===id?{...item,read_at:readAt}:item))}if(loading)return <div className="grid place-items-center py-24 text-white"><span className="text-sm text-gray-400">Loading notifications...</span></div>;if(error)return <div className="card rounded-3xl p-8 text-center"><p role="alert" className="text-sm font-semibold text-rose-200">{error}</p></div>;const unreadCount=notifications.filter(x=>!x.read_at).length;return <div className="grid gap-6"><div><h1 className="text-2xl font-black text-white">Notifications</h1><p className="mt-1 text-sm text-gray-400">{unreadCount?`${unreadCount} unread ${unreadCount===1?"notification":"notifications"}`:"You're all caught up."}</p></div>{notifications.length===0?<div className="card rounded-3xl p-8 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-200"><FaBell/></span><h2 className="mt-4 text-xl font-black text-white">No notifications yet</h2><p className="mt-2 text-sm text-gray-400">Updates about your projects and support tickets will appear here.</p></div>:<div className="grid gap-3">{notifications.map(n=>{const unread=!n.read_at;return <article key={n.id} className={`card flex flex-col gap-3 rounded-3xl p-5 sm:flex-row sm:items-center sm:justify-between ${unread?"border-cyan-300/30 bg-cyan-300/[0.06]":""}`}><div className="flex min-w-0 items-start gap-4"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-cyan-300/10 text-cyan-200">{unread?<FaEnvelopeOpenText/>:<FaBell/>}</span><div><p className="text-sm font-black text-white">{formatNotificationType(n.type)}</p><p className="mt-1 text-sm leading-6 text-gray-300">{extractNotificationText(n.payload)}</p><p className="mt-1 text-xs text-gray-600">{formatNotificationDate(n.created_at)}</p></div></div>{unread&&<button type="button" onClick={()=>handleMarkRead(n.id)} disabled={markingId===n.id} className="btn btn-secondary shrink-0"><FaCheck className="mr-2"/>{markingId===n.id?"Marking...":"Mark as read"}</button>}</article>})}</div>}</div>}
