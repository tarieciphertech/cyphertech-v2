import { useEffect,useState } from "react";
import { Link } from "react-router-dom";
import { FaBriefcase,FaCalendarAlt,FaFolderOpen } from "react-icons/fa";
import { supabase } from "../../lib/supabase";
import { useAuth } from "../../auth/useAuth";
const STATUS_STYLES={planned:"border-cyan-300/30 bg-cyan-300/10 text-cyan-200",active:"border-emerald-300/30 bg-emerald-300/10 text-emerald-200",on_hold:"border-amber-300/30 bg-amber-300/10 text-amber-200",completed:"border-indigo-300/30 bg-indigo-300/10 text-indigo-200",cancelled:"border-rose-300/30 bg-rose-300/10 text-rose-200"};const STATUS_LABELS={planned:"Planned",active:"Active",on_hold:"On hold",completed:"Completed",cancelled:"Cancelled"};const formatDate=value=>{if(!value)return"—";const d=new Date(value);return Number.isNaN(d.getTime())?"—":d.toLocaleDateString()};
export default function ClientProjects(){const{user}=useAuth();const[projects,setProjects]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{let active=true;async function load(){if(!supabase||!user){setLoading(false);return}const{data,error:e}=await supabase.from("projects").select("id,title,description,status,start_date,end_date,created_at").order("created_at",{ascending:false});if(!active)return;if(e){setError("We could not load your projects right now. Please try again.");setProjects([])}else setProjects(data||[]);setLoading(false)}load();return()=>{active=false}},[user]);if(loading)return <div className="grid place-items-center py-24 text-white"><span className="text-sm text-gray-400">Loading your projects...</span></div>;if(error)return <div className="card rounded-3xl p-8 text-center"><p role="alert" className="text-sm font-semibold text-rose-200">{error}</p></div>;if(projects.length===0)return <div className="card rounded-3xl p-8 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-200"><FaFolderOpen className="text-2xl"/></span><h2 className="mt-4 text-xl font-black text-white">No projects yet</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-gray-400">When a project is created for you, it will appear here.</p></div>;return <div className="grid gap-6"><div><h1 className="text-2xl font-black text-white">Your projects</h1><p className="mt-1 text-sm text-gray-400">{projects.length} {projects.length===1?"project":"projects"}</p></div><div className="grid gap-4 md:grid-cols-2">{projects.map(project=><Link key={project.id} to={`/client/projects/${project.id}`} className="card block rounded-3xl p-6 transition hover:-translate-y-0.5"><div className="flex items-start justify-between gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-cyan-300/10 text-cyan-200"><FaBriefcase/></span><span className={`rounded-full border px-3 py-1 text-xs font-black uppercase tracking-wide ${STATUS_STYLES[project.status]||STATUS_STYLES.planned}`}>{STATUS_LABELS[project.status]||project.status}</span></div><h2 className="mt-4 text-lg font-black text-white">{project.title}</h2><p className="mt-2 text-sm leading-6 text-gray-400">{project.description||"No description provided."}</p><div className="mt-5 grid grid-cols-2 gap-3 border-t border-white/10 pt-4 text-xs"><div><span className="font-black uppercase tracking-wide text-gray-500">Start</span><p className="mt-1 flex items-center gap-1.5 font-bold text-gray-200"><FaCalendarAlt/>{formatDate(project.start_date)}</p></div><div><span className="font-black uppercase tracking-wide text-gray-500">End</span><p className="mt-1 flex items-center gap-1.5 font-bold text-gray-200"><FaCalendarAlt/>{formatDate(project.end_date)}</p></div></div></Link>)}</div></div>}
